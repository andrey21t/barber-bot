BOT_TOKEN=
DATABASE_URL=sqlite+aiosqlite:///./barber.db
# Sync engine for SQLAlchemyJobStore (APScheduler, prod) and alembic migrations.
# Empty in dev (MemoryJobStore). In prod Render sets DATABASE_URL=postgresql+asyncpg://...
# (async); env.py converts to postgresql+psycopg2:// for sync alembic.
DATABASE_URL_SYNC=
ADMIN_ID=
